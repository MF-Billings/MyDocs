// Use $broadcast and $emit to propagate events to the descendents and ancestors (respectively) of the current controller's scope.
// $on is used to define the response to these events

// where possible you should store shared information on a common ancestor through the alias where possible