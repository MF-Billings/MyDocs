<?php
// convert to date
$date_from_string = date_format(date_create(string_of_date), string $format)

// Convert a single item or object to an array. Note that this likely won't work for nested arrays.
$array = (array) obj;

// TODOmb write method to avoid checking if object is array before casting